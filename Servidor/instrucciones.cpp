#include <iostream>
#include <string>

void obtainInst(){
    /*        elif entrada == "descargar":
            archivo = input("Entre el nombre del archivo")
            print(entrada+" "+archivo)
        elif entrada == "buscar":
            archivo = input("Entre el nombre del archivo")
            print(entrada+" "+archivo)
        elif entrada == "unirse":
            archivo = input("Entre el nombre del archivo")
            print(entrada+" "+archivo)
    */
    std::string entrada;
    if(entrada =="descargar"){

    }else if (entrada == "buscar")
    {
        /* code */
    }else if (entrada == "unirse")
    {
        /* code */
    }else{
        std::cout<<"Comando incorrecto"<<std::endl;
    }
    
    
}